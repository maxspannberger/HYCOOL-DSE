# Routing factor, segment generation, inclination angles


def build_segments(L, n_seg, routing_factor=1.3, inclination_deg=0.0):
    # Returns list of segment dicts: {dz, theta_rad}.
    # routing_factor 1.2–1.4 accounts for real installation path vs straight-line L.
    raise NotImplementedError
